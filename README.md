# SKALDEngine Backend Part

**SKALD Engine** — это платформа для интерактивного сторителлинга и ролевой игры с ИИ-персонажами, использующая архитектуру микросервисов, RAG (Retrieval Augmented Generation) для долгосрочной памяти и систему Supervisor-Worker для управления сюжетом.

## ✨ Особенности

- **Иммерсивный ролеплей:** Глубокое погружение благодаря продвинутым системным промптам, стилизации ответов и генерации атмосферных описаний.
- **Два режима игры:**
  - **Песочница:** Свободная игра без ограничений.
  - **Сценарий:** Игра по заданному сюжету (от точки А к точке Б) с контролем прохождения ключевых событий.
- **Умная память (RAG):** Персонажи помнят факты из своей биографии и лор мира благодаря векторному поиску (ChromaDB).
- **AI-Супервизор:** Отдельная нейросеть-арбитр следит за логикой повествования и подталкивает игрока, если история "буксует" или уходит от сценария.
- **Управление реальностью:** Возможность перегенерировать ответ ИИ (Swipe), создавая альтернативные ветки развития событий.
- **Система Персон:** Создание и сохранение разных профилей игрока ("Персон") для разных историй и вселенных.

## 🛠 Технический стек

- **Язык:** Python 3.11+
- **Фреймворк:** FastAPI
- **Архитектура:** Microservices (Auth Service + Core Service)
- **Базы данных:**
  - **PostgreSQL** (в Docker) — основное хранилище пользователей, сессий и истории.
  - **ChromaDB** — векторная база данных для RAG.
- **ORM / Миграции:** SQLAlchemy (Async), Alembic.
- **AI & ML:**
  - **Main LLM:** Google Gemini 3.0 Flash (генерация ответов).
  - **Supervisor LLM:** Gemini 2.5 Flash (контроль сюжета).
  - **Embeddings:** Google Gemini Embeddings.

## 🏗 Архитектура сервисов

Проект разделен на независимые сервисы, работающие в связке:

| Сервис | Порт | Описание | База данных |
| :--- | :--- | :--- | :--- |
| **Auth Service** | `:8001` | Регистрация, Логин, Выдача JWT токенов | PostgreSQL (`auth_db`) |
| **Core Service** | `:8000` | Игровая логика, Чат, RAG, Персонажи | PostgreSQL (`core_db`) + ChromaDB |

## Быстрый старт

### 1. Запуск инфраструктуры
Мы используем Docker Compose для поднятия двух изолированных баз данных PostgreSQL (для Auth и Core сервисов).

```bash
docker compose up -d
```

### 2. Настройка Auth Service
В отдельном терминале:

```bash
cd services/auth_service

# 1. Создаем окружение и ставим зависимости
python -m venv venv
source venv/bin/activate  # (Windows: venv\Scripts\activate)
pip install -r requirements.txt

# 2. Настраиваем .env (см. .env.example)

# 3. Применяем миграции
python -m alembic upgrade head

# 4. Запускаем сервис
uvicorn app.main:app --reload --port 8001
```

### 3. Настройка Core Service
В другом терминале:

```bash
cd services/core_service

# 1. Создаем окружение и ставим зависимости
python -m venv venv
source venv/bin/activate
pip install -r requirements.txt

# 2. Настраиваем .env (ОБЯЗАТЕЛЬНО укажите GEMINI_API_KEY)

# 3. Применяем миграции
python -m alembic upgrade head

# 4. Запускаем сервис
uvicorn app.main:app --reload --port 8000
```

## Документация API

Документация (Swagger UI) доступна по адресам:
- **Core Service:** [http://127.0.0.1:8000/docs](http://127.0.0.1:8000/docs) (Основной функционал)
- **Auth Service:** [http://127.0.0.1:8001/docs](http://127.0.0.1:8001/docs) (Регистрация пользователей)

### Как начать игру (Тест):
1. Зарегистрируйтесь через Auth Service (`POST /auth/register`).
2. Получите токен (`POST /auth/login`) и скопируйте `access_token`.
3. Перейдите в Core Service, нажмите кнопку **Authorize** и вставьте токен.
4. Создайте Персону (`POST /personas`).
5. Начните Сессию (`POST /sessions`).